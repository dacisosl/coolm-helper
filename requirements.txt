# 기본 실행
PyQt6>=6.6
pywinauto>=0.6.9   # ⚡ 간편 등록 (현재 보고 있는 쪽지 읽기, UI 자동화)

# Plan B 폴백 (엑셀 내보내기 파싱)
xlrd>=2.0

# 구글 캘린더 연동 (v1.4부터 기본 동봉 — 설정에서 칩 버튼 한 번으로 로그인)
google-api-python-client>=2.100
google-auth-oauthlib>=1.0

# 개발·배포용
# pyinstaller
